namespace Cs2DemoKit.Analysis.Plugins.Markers;

/// <summary>
///     Empty marker type identifying the <c>CCSGameRules.m_bFreezePeriod</c> entity field for
///     <c>EntityValueChangedEvent&lt;TMarker&gt;</c>. Each (entity class, field path) pair has
///     its own marker so the evaluator's dispatch index assigns one slot per subscription.
/// </summary>
public sealed class CCSGameRulesFreezePeriodMarker
{
}
